class FormFieldOptions {
  constructor(fieldId, inputId, labelText, inputEl) {
    this.fieldId = fieldId;
    this.inputId = inputId;
    this.labelText = labelText;
    this.inputEl = inputEl;
  }
}

export default FormFieldOptions;
